name: Hello world

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build steps
        run: echo "my-build"
  test: 
     runs-on: ubuntu-latest
     needs: build
     steps:
       - name: test-one
         run: echo "my-test"

  deploy:
     runs-on: ubuntu-latest
     needs: test
     steps:
       - name: Deploy-one
         run: echo "my-deploy"
       
  
